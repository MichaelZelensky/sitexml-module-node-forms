
# SiteXML forms module

Generates HTML form.

## Options (site.json)

```
{
    "id": "instanceid",
    "fields": [
        {
            "name": "field name (INPUT @name argument)",
            "caption": "any string",
            "type": "field type (INPUT @type argument)"
        }
    ]
}
```